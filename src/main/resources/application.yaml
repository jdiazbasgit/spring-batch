
spring:
  mvc:
    static-path-pattern: /resources
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
  resources:
    static-locations: /resources/**
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/batch?useUnicode=true&characterEncoding=utf8&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
      username: curso
      password: Cursocurso1; 
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      use-new-id-generator-mappings: false

  batch:
    jdbc:
      initialize-schema: "always"
 
      
file:
  input: "entrada.txt"

